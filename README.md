# Game of Life

A browser-based Conway's Game of Life simulator. Load patterns from a built-in library, pan and zoom the canvas, and watch the simulation run in parallel across Web Workers using sparse computation.


## Tests

```
node --test life-submodel.test.js figure-parser.test.js
```
